/**
 * Shopify API Routes.
 *
 * Exposes webhook endpoints (HMAC-verified), OAuth flow,
 * store management, data access, and conversation context.
 */
import { FastifyInstance } from 'fastify';
export declare function shopifyRoutes(fastify: FastifyInstance): Promise<void>;
//# sourceMappingURL=shopify.routes.d.ts.map